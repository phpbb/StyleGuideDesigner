{# forum.twig #}
{% extends "./layout/layout.twig" %}

{% block component %}
				<ul class="c-list">
					<li class="c-list-header">
						<h2 class="c-list-header-title">Announcments</h2>
						<ul class="c-list-header-actions">
							<li class="c-list-header-action">
								<a class="c-list-header-action-link" href="" data-toggle="tooltip" data-placement="top" title="SubForums">
									<svg class="o-icon c-list-header-action-link-icon"><use xlink:href="#more-v"></use></svg>
								</a>
							</li>
						</ul>
					</li>
				{% for topic in topics %}
				{% if topic.type === "announce" %}
					{% include './includes/list-item.twig' %}
				{% endif %}
				{% endfor %}
				</ul>
				<ul class="c-list">
					<li class="c-list-header">
						<h2 class="c-list-header-title">Forum</h2>
						<ul class="c-list-header-actions">
							<li class="c-list-header-action">
								<a class="c-list-header-action-link" href="" data-toggle="tooltip" data-placement="top" title="SubForums">
									<svg class="o-icon c-list-header-action-link-icon"><use xlink:href="#more-v"></use></svg>
								</a>
							</li>
						</ul>
					</li>
					{% for topic in topics %}
					{% if topic.type === "sticky" %}
						{% include './includes/list-item.twig' %}
					{% endif %}
					{% endfor %}
					{% for topic in topics %}
					{% if topic.type === "topic" %}
						{% include './includes/list-item.twig' %}
					{% endif %}
					{% endfor %}
				</ul>
{% endblock %}
